// SideStore release — temporary physical-device testing through the backend-only Cloudflare Quick Tunnel.
// Routes are served at the origin root (no /api prefix).
API_BASE_URL = https:/$()/novels-west-architect-oklahoma.trycloudflare.com
